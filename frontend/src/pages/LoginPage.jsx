// Redirect shim — the real LoginPage is now at pages/auth/LoginPage.jsx
// This file exists only to prevent import errors from any remaining legacy references.
export { default } from './auth/LoginPage';
